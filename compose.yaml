services:
  frontend:
    build:
      context: ./client
      dockerfile: dockerfile
    ports:
      - "8080:80"  
    networks:
      - app-network

  backend:
    build:
      context: ./service
      dockerfile: dockerfile
    ports:
      - "9090:9090"  
    networks:
      - app-network
networks:
  app-network:
    driver: bridge
